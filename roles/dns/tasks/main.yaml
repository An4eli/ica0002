- name: Stop service
  ansible.builtin.service:
    name: systemd-resolved
    state: stopped
    enabled: no

- name: Move
  ansible.builtin.template:
     src: resolv.conf
     dest: /etc/resolv.conf
